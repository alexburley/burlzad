import React from "react";

export default function WhipsnapContent() {
  return (
    <p>
      <a href="https://www.npmjs.com/package/whipsnap">Whipsnap</a> is a rather
      simple CMS that I ended up building over the course of a year, I had this
      idea of building my own CMS to host a blog. I am not a blogger but it felt
      like a worthwhile project, especially since I was learning JS at the time.
      It's inspired by Jekyll, you drag markdown files into a folder and let
      just run build on the package for it to render your content. It provides
      support for static pages as well.
    </p>
  );
}
